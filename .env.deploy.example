# Local Deployment Configuration
# Copy this file to .env.deploy and fill in your values
#
# Usage:
#   cp .env.deploy.example .env.deploy
#   # Edit .env.deploy with your values
#   ./scripts/deploy.sh
#

# ==========================================
# Required: SSH Connection
# ==========================================

# Server IP address or hostname
SSH_HOST=your-server-ip-or-hostname

# SSH username (e.g., ubuntu, root, deployer)
SSH_USERNAME=ubuntu

# Path to SSH private key (default: ~/.ssh/id_rsa)
# This should be the key whose public key is in the server's authorized_keys
SSH_PRIVATE_KEY=~/.ssh/id_rsa

# SSH port (default: 22)
SSH_PORT=22

# ==========================================
# Optional: Deployment Configuration
# ==========================================

# Project path on server (default: /projects/<repo-name>)
# PROJECT_PATH=/projects/learning-algo

# Path to .env file to deploy to server (optional)
# This file will be copied to the server as .env
# ENV_FILE=.env.production

# ==========================================
# Optional: External Services
# (Only needed if not using Docker services)
# ==========================================

# PostgreSQL
# DB_HOST=your-postgres-host.com
# DB_PORT=5432
# DB_USER=your_user
# DB_PASSWORD=your_password
# DB_NAME=your_database

# Elasticsearch
# ES_URL=https://your-elasticsearch-host.com:9200

# Redis
# REDIS_URL=redis://your-redis-host.com:6379

# Kibana
# KIBANA_URL=https://your-kibana-host.com:5601

# ==========================================
# Optional: CORS Configuration
# ==========================================

# Allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS=https://learn.arvaibhav.cloud,https://learn-api.arvaibhav.cloud
